import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'skillnest-school-frontend',
  webDir: 'www'
};

export default config;
